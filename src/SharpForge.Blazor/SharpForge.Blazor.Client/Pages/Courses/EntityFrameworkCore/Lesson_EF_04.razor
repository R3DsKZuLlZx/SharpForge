@page "/training/entity-framework-core/lesson/4"
<PageTitle>Migrations - Entity Framework Core - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <aside class="lesson-sidebar">
        <div class="sidebar-header"><a href="/training/entity-framework-core" class="back-link"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>Course Overview</a><h3>Entity Framework Core</h3></div>
        <nav class="lesson-nav"><a href="/training/entity-framework-core/lesson/1" class="lesson-nav-item"><span class="lesson-num">1</span><span>Introduction</span></a><a href="/training/entity-framework-core/lesson/2" class="lesson-nav-item"><span class="lesson-num">2</span><span>DbContext</span></a><a href="/training/entity-framework-core/lesson/3" class="lesson-nav-item"><span class="lesson-num">3</span><span>Entity Models</span></a><a href="/training/entity-framework-core/lesson/4" class="lesson-nav-item active"><span class="lesson-num">4</span><span>Migrations</span></a><a href="/training/entity-framework-core/lesson/5" class="lesson-nav-item"><span class="lesson-num">5</span><span>CRUD</span></a><a href="/training/entity-framework-core/lesson/6" class="lesson-nav-item"><span class="lesson-num">6</span><span>LINQ</span></a><a href="/training/entity-framework-core/lesson/7" class="lesson-nav-item"><span class="lesson-num">7</span><span>Relationships</span></a><a href="/training/entity-framework-core/lesson/8" class="lesson-nav-item"><span class="lesson-num">8</span><span>Loading Data</span></a><a href="/training/entity-framework-core/lesson/9" class="lesson-nav-item"><span class="lesson-num">9</span><span>Change Tracking</span></a><a href="/training/entity-framework-core/lesson/10" class="lesson-nav-item"><span class="lesson-num">10</span><span>Transactions</span></a><a href="/training/entity-framework-core/lesson/11" class="lesson-nav-item"><span class="lesson-num">11</span><span>Concurrency</span></a><a href="/training/entity-framework-core/lesson/12" class="lesson-nav-item"><span class="lesson-num">12</span><span>Performance</span></a></nav>
    </aside>
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 4</span><h1>Migrations</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>30 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>What Are Migrations?</h2>
                    <p>Migrations are a way to incrementally update your database schema to keep it in sync with your data model while preserving existing data.</p>
                </section>

                <section class="lesson-section">
                    <h2>Creating Migrations</h2>
                    <pre><code class="language-bash"># Create initial migration
dotnet ef migrations add InitialCreate

# Create migration for new changes
dotnet ef migrations add AddProductDescription

# Specify project (if multiple projects)
dotnet ef migrations add AddOrders -p Data -s WebApi</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Migration File Structure</h2>
                    <pre><code class="language-csharp">public partial class AddProductDescription : Migration
{
    protected override void Up(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.AddColumn&lt;string&gt;(
            name: "Description",
            table: "Products",
            type: "nvarchar(500)",
            maxLength: 500,
            nullable: true);
    }

    protected override void Down(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.DropColumn(
            name: "Description",
            table: "Products");
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Applying Migrations</h2>
                    <pre><code class="language-bash"># Apply all pending migrations
dotnet ef database update

# Apply to specific migration
dotnet ef database update AddProductDescription

# Rollback to previous migration
dotnet ef database update InitialCreate

# Generate SQL script instead of applying
dotnet ef migrations script -o migration.sql

# Idempotent script (can run multiple times)
dotnet ef migrations script --idempotent</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Managing Migrations</h2>
                    <pre><code class="language-bash"># List migrations
dotnet ef migrations list

# Remove last migration (if not applied)
dotnet ef migrations remove

# Check for pending migrations in code
if (context.Database.GetPendingMigrations().Any())
{
    context.Database.Migrate();
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Custom Migration Operations</h2>
                    <pre><code class="language-csharp">protected override void Up(MigrationBuilder migrationBuilder)
{
    // Add column
    migrationBuilder.AddColumn&lt;string&gt;("Sku", "Products", nullable: true);
    
    // Create index
    migrationBuilder.CreateIndex("IX_Products_Sku", "Products", "Sku");
    
    // Execute raw SQL
    migrationBuilder.Sql("UPDATE Products SET Sku = 'SKU-' + CAST(Id AS VARCHAR)");
    
    // Create stored procedure
    migrationBuilder.Sql(
        "CREATE PROCEDURE GetProductsByCategoryId " +
        "@@CategoryId INT AS BEGIN " +
        "SELECT * FROM Products WHERE CategoryId = @@CategoryId END");
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Key Takeaways</h2>
                    <div class="key-takeaways">
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Migrations track database schema changes over time.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Up() applies changes, Down() reverts them.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Use scripts for production deployments.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Always commit migrations to source control.</span></div>
                    </div>
                </section>
            </div>
            <LessonFooter 
                PreviousUrl="/training/entity-framework-core/lesson/3" 
                PreviousText="Previous Lesson"
                NextUrl="/training/entity-framework-core/lesson/5" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

