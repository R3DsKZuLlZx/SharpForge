@page "/training/entity-framework-core/lesson/9"
<PageTitle>Change Tracking - Entity Framework Core - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <aside class="lesson-sidebar">
        <div class="sidebar-header"><a href="/training/entity-framework-core" class="back-link"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>Course Overview</a><h3>Entity Framework Core</h3></div>
        <nav class="lesson-nav"><a href="/training/entity-framework-core/lesson/1" class="lesson-nav-item"><span class="lesson-num">1</span><span>Introduction</span></a><a href="/training/entity-framework-core/lesson/2" class="lesson-nav-item"><span class="lesson-num">2</span><span>DbContext</span></a><a href="/training/entity-framework-core/lesson/3" class="lesson-nav-item"><span class="lesson-num">3</span><span>Entity Models</span></a><a href="/training/entity-framework-core/lesson/4" class="lesson-nav-item"><span class="lesson-num">4</span><span>Migrations</span></a><a href="/training/entity-framework-core/lesson/5" class="lesson-nav-item"><span class="lesson-num">5</span><span>CRUD</span></a><a href="/training/entity-framework-core/lesson/6" class="lesson-nav-item"><span class="lesson-num">6</span><span>LINQ</span></a><a href="/training/entity-framework-core/lesson/7" class="lesson-nav-item"><span class="lesson-num">7</span><span>Relationships</span></a><a href="/training/entity-framework-core/lesson/8" class="lesson-nav-item"><span class="lesson-num">8</span><span>Loading Data</span></a><a href="/training/entity-framework-core/lesson/9" class="lesson-nav-item active"><span class="lesson-num">9</span><span>Change Tracking</span></a><a href="/training/entity-framework-core/lesson/10" class="lesson-nav-item"><span class="lesson-num">10</span><span>Transactions</span></a><a href="/training/entity-framework-core/lesson/11" class="lesson-nav-item"><span class="lesson-num">11</span><span>Concurrency</span></a><a href="/training/entity-framework-core/lesson/12" class="lesson-nav-item"><span class="lesson-num">12</span><span>Performance</span></a></nav>
    </aside>
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 9</span><h1>Change Tracking</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>25 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Entity States</h2>
                    <pre><code class="language-csharp">// Detached - Not tracked by the context
var product = new Product { Name = "Widget" };

// Added - Will be inserted
context.Products.Add(product);  // State = Added

// Unchanged - Loaded from DB, no changes
var loaded = await context.Products.FindAsync(1);  // State = Unchanged

// Modified - Tracked entity with changes
loaded.Price = 99.99m;  // State = Modified

// Deleted - Will be deleted
context.Products.Remove(loaded);  // State = Deleted</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Checking Entity State</h2>
                    <pre><code class="language-csharp">var entry = context.Entry(product);
Console.WriteLine(entry.State);  // EntityState enum

// Check modified properties
if (entry.State == EntityState.Modified)
{
    foreach (var prop in entry.Properties)
    {
        if (prop.IsModified)
        {
            Console.WriteLine($"{prop.Metadata.Name}: {prop.OriginalValue} -> {prop.CurrentValue}");
        }
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>No-Tracking Queries</h2>
                    <pre><code class="language-csharp">// Read-only queries (better performance)
var products = await context.Products
    .AsNoTracking()
    .ToListAsync();

// Changes won't be detected
products[0].Price = 0;  // Won't be saved!

// Configure globally for read-heavy scenarios
context.ChangeTracker.QueryTrackingBehavior = QueryTrackingBehavior.NoTracking;

// Or in DbContext
protected override void OnConfiguring(DbContextOptionsBuilder options)
{
    options.UseQueryTrackingBehavior(QueryTrackingBehavior.NoTracking);
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Attaching Entities</h2>
                    <pre><code class="language-csharp">// Attach disconnected entity (State = Unchanged)
context.Products.Attach(product);

// Attach as modified (will update all properties)
context.Products.Update(product);

// Attach and mark specific properties modified
context.Products.Attach(product);
context.Entry(product).Property(p => p.Price).IsModified = true;</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>ChangeTracker Debugging</h2>
                    <pre><code class="language-csharp">// View all tracked entities
Console.WriteLine(context.ChangeTracker.DebugView.LongView);

// Count tracked entities
var tracked = context.ChangeTracker.Entries().Count();

// Get pending changes
var added = context.ChangeTracker.Entries()
    .Where(e => e.State == EntityState.Added)
    .ToList();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Key Takeaways</h2>
                    <div class="key-takeaways">
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>EF Core tracks entity states: Added, Modified, Deleted, Unchanged.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Use AsNoTracking() for read-only queries.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Attach/Update for disconnected scenarios.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>DebugView helps troubleshoot tracking issues.</span></div>
                    </div>
                </section>
            </div>
            <nav class="lesson-navigation"><a href="/training/entity-framework-core/lesson/8" class="btn btn-secondary"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>Previous</a><a href="/training/entity-framework-core/lesson/10" class="btn btn-primary">Next: Transactions<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg></a></nav>
        </article>
    </main>
</div>

