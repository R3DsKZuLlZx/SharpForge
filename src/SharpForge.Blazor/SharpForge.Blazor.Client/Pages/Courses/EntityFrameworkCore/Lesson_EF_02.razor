@page "/training/entity-framework-core/lesson/2"
<PageTitle>Setting Up DbContext - Entity Framework Core - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <aside class="lesson-sidebar">
        <div class="sidebar-header"><a href="/training/entity-framework-core" class="back-link"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>Course Overview</a><h3>Entity Framework Core</h3></div>
        <nav class="lesson-nav"><a href="/training/entity-framework-core/lesson/1" class="lesson-nav-item"><span class="lesson-num">1</span><span>Introduction</span></a><a href="/training/entity-framework-core/lesson/2" class="lesson-nav-item active"><span class="lesson-num">2</span><span>DbContext</span></a><a href="/training/entity-framework-core/lesson/3" class="lesson-nav-item"><span class="lesson-num">3</span><span>Entity Models</span></a><a href="/training/entity-framework-core/lesson/4" class="lesson-nav-item"><span class="lesson-num">4</span><span>Migrations</span></a><a href="/training/entity-framework-core/lesson/5" class="lesson-nav-item"><span class="lesson-num">5</span><span>CRUD</span></a><a href="/training/entity-framework-core/lesson/6" class="lesson-nav-item"><span class="lesson-num">6</span><span>LINQ</span></a><a href="/training/entity-framework-core/lesson/7" class="lesson-nav-item"><span class="lesson-num">7</span><span>Relationships</span></a><a href="/training/entity-framework-core/lesson/8" class="lesson-nav-item"><span class="lesson-num">8</span><span>Loading Data</span></a><a href="/training/entity-framework-core/lesson/9" class="lesson-nav-item"><span class="lesson-num">9</span><span>Change Tracking</span></a><a href="/training/entity-framework-core/lesson/10" class="lesson-nav-item"><span class="lesson-num">10</span><span>Transactions</span></a><a href="/training/entity-framework-core/lesson/11" class="lesson-nav-item"><span class="lesson-num">11</span><span>Concurrency</span></a><a href="/training/entity-framework-core/lesson/12" class="lesson-nav-item"><span class="lesson-num">12</span><span>Performance</span></a></nav>
    </aside>
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 2</span><h1>Setting Up DbContext</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>25 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Creating a DbContext</h2>
                    <pre><code class="language-csharp">public class AppDbContext : DbContext
{
    public AppDbContext(DbContextOptions&lt;AppDbContext&gt; options) 
        : base(options)
    {
    }

    public DbSet&lt;Product&gt; Products =&gt; Set&lt;Product&gt;();
    public DbSet&lt;Category&gt; Categories =&gt; Set&lt;Category&gt;();
    public DbSet&lt;Order&gt; Orders =&gt; Set&lt;Order&gt;();
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Registering in ASP.NET Core</h2>
                    <pre><code class="language-csharp">// Program.cs
builder.Services.AddDbContext&lt;AppDbContext&gt;(options =&gt;
    options.UseSqlServer(
        builder.Configuration.GetConnectionString("DefaultConnection")));</code></pre>
                    <pre><code class="language-json">// appsettings.json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=MyAppDb;Trusted_Connection=True;"
  }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Different Database Providers</h2>
                    <pre><code class="language-csharp">// SQL Server
options.UseSqlServer(connectionString);

// PostgreSQL
options.UseNpgsql(connectionString);

// SQLite
options.UseSqlite("Data Source=app.db");

// In-Memory (for testing)
options.UseInMemoryDatabase("TestDb");</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>OnModelCreating</h2>
                    <pre><code class="language-csharp">public class AppDbContext : DbContext
{
    // ... constructor and DbSets

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        // Configure entities using Fluent API
        modelBuilder.Entity&lt;Product&gt;(entity =&gt;
        {
            entity.HasKey(e =&gt; e.Id);
            entity.Property(e =&gt; e.Name).IsRequired().HasMaxLength(100);
            entity.Property(e =&gt; e.Price).HasPrecision(18, 2);
        });

        // Seed data
        modelBuilder.Entity&lt;Category&gt;().HasData(
            new Category { Id = 1, Name = "Electronics" },
            new Category { Id = 2, Name = "Clothing" }
        );
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>DbContext Lifetime</h2>
                    <div class="info-box">
                        <div class="info-box-header"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg><span>Important</span></div>
                        <p>DbContext is registered as <strong>Scoped</strong> by default. This means one instance per HTTP request. Never use Singleton for DbContext!</p>
                    </div>
                    <pre><code class="language-csharp">// Default (recommended)
builder.Services.AddDbContext&lt;AppDbContext&gt;(...);  // Scoped

// For background services, use DbContextFactory
builder.Services.AddDbContextFactory&lt;AppDbContext&gt;(...);</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Key Takeaways</h2>
                    <div class="key-takeaways">
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>DbContext is the main class for database operations.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Use AddDbContext in ASP.NET Core for DI registration.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>OnModelCreating configures entities with Fluent API.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>DbContext should be Scoped, not Singleton.</span></div>
                    </div>
                </section>
            </div>
            <LessonFooter 
                PreviousUrl="/training/entity-framework-core/lesson/1" 
                PreviousText="Previous Lesson"
                NextUrl="/training/entity-framework-core/lesson/3" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

