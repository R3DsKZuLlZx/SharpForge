@page "/training/entity-framework-core/lesson/6"
<PageTitle>Querying with LINQ - Entity Framework Core - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <aside class="lesson-sidebar">
        <div class="sidebar-header"><a href="/training/entity-framework-core" class="back-link"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>Course Overview</a><h3>Entity Framework Core</h3></div>
        <nav class="lesson-nav"><a href="/training/entity-framework-core/lesson/1" class="lesson-nav-item"><span class="lesson-num">1</span><span>Introduction</span></a><a href="/training/entity-framework-core/lesson/2" class="lesson-nav-item"><span class="lesson-num">2</span><span>DbContext</span></a><a href="/training/entity-framework-core/lesson/3" class="lesson-nav-item"><span class="lesson-num">3</span><span>Entity Models</span></a><a href="/training/entity-framework-core/lesson/4" class="lesson-nav-item"><span class="lesson-num">4</span><span>Migrations</span></a><a href="/training/entity-framework-core/lesson/5" class="lesson-nav-item"><span class="lesson-num">5</span><span>CRUD</span></a><a href="/training/entity-framework-core/lesson/6" class="lesson-nav-item active"><span class="lesson-num">6</span><span>LINQ</span></a><a href="/training/entity-framework-core/lesson/7" class="lesson-nav-item"><span class="lesson-num">7</span><span>Relationships</span></a><a href="/training/entity-framework-core/lesson/8" class="lesson-nav-item"><span class="lesson-num">8</span><span>Loading Data</span></a><a href="/training/entity-framework-core/lesson/9" class="lesson-nav-item"><span class="lesson-num">9</span><span>Change Tracking</span></a><a href="/training/entity-framework-core/lesson/10" class="lesson-nav-item"><span class="lesson-num">10</span><span>Transactions</span></a><a href="/training/entity-framework-core/lesson/11" class="lesson-nav-item"><span class="lesson-num">11</span><span>Concurrency</span></a><a href="/training/entity-framework-core/lesson/12" class="lesson-nav-item"><span class="lesson-num">12</span><span>Performance</span></a></nav>
    </aside>
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 6</span><h1>Querying with LINQ</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>35 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Filtering with Where</h2>
                    <pre><code class="language-csharp">// Simple filter
var expensiveProducts = await context.Products
    .Where(p =&gt; p.Price &gt; 100)
    .ToListAsync();

// Multiple conditions
var activeExpensive = await context.Products
    .Where(p =&gt; p.Price &gt; 100 &amp;&amp; p.IsActive)
    .ToListAsync();

// String operations
var laptops = await context.Products
    .Where(p =&gt; p.Name.Contains("Laptop"))
    .ToListAsync();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Sorting</h2>
                    <pre><code class="language-csharp">// Ascending
var byName = await context.Products
    .OrderBy(p =&gt; p.Name)
    .ToListAsync();

// Descending
var byPriceDesc = await context.Products
    .OrderByDescending(p =&gt; p.Price)
    .ToListAsync();

// Multiple sort criteria
var sorted = await context.Products
    .OrderBy(p =&gt; p.Category.Name)
    .ThenByDescending(p =&gt; p.Price)
    .ToListAsync();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Pagination</h2>
                    <pre><code class="language-csharp">int page = 1;
int pageSize = 10;

var pagedProducts = await context.Products
    .OrderBy(p =&gt; p.Name)
    .Skip((page - 1) * pageSize)
    .Take(pageSize)
    .ToListAsync();

var totalCount = await context.Products.CountAsync();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Projection with Select</h2>
                    <pre><code class="language-csharp">// Select specific columns (more efficient)
var productNames = await context.Products
    .Select(p =&gt; p.Name)
    .ToListAsync();

// Project to DTO
var productDtos = await context.Products
    .Select(p =&gt; new ProductDto
    {
        Id = p.Id,
        Name = p.Name,
        Price = p.Price,
        CategoryName = p.Category.Name
    })
    .ToListAsync();

// Anonymous type
var summary = await context.Products
    .Select(p =&gt; new { p.Name, p.Price })
    .ToListAsync();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Grouping and Aggregates</h2>
                    <pre><code class="language-csharp">// Group by category
var byCategory = await context.Products
    .GroupBy(p =&gt; p.CategoryId)
    .Select(g =&gt; new
    {
        CategoryId = g.Key,
        Count = g.Count(),
        TotalValue = g.Sum(p =&gt; p.Price),
        AveragePrice = g.Average(p =&gt; p.Price)
    })
    .ToListAsync();

// Aggregates
var maxPrice = await context.Products.MaxAsync(p =&gt; p.Price);
var minPrice = await context.Products.MinAsync(p =&gt; p.Price);
var count = await context.Products.CountAsync();
var sum = await context.Products.SumAsync(p =&gt; p.Stock);</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Query vs Method Syntax</h2>
                    <pre><code class="language-csharp">// Method syntax (more common)
var products = await context.Products
    .Where(p =&gt; p.Price &gt; 100)
    .OrderBy(p =&gt; p.Name)
    .ToListAsync();

// Query syntax (SQL-like)
var products = await (
    from p in context.Products
    where p.Price &gt; 100
    orderby p.Name
    select p
).ToListAsync();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Key Takeaways</h2>
                    <div class="takeaways">
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Where filters, OrderBy sorts, Skip/Take paginates.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Use Select to project only needed columns.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>GroupBy with aggregates for reporting queries.</span></div>
                        <div class="takeaway"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg><span>Queries are translated to SQL—check the generated SQL!</span></div>
                    </div>
                </section>
            </div>
            <LessonFooter 
                PreviousUrl="/training/entity-framework-core/lesson/5" 
                PreviousText="Previous Lesson"
                NextUrl="/training/entity-framework-core/lesson/7" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

