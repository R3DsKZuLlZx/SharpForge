@page "/training/async-concurrency/lesson/10"
<PageTitle>PLINQ - Parallel LINQ - Async/Await & Concurrency - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/async-concurrency"
        CourseTitle="Async/Await & Concurrency"
        CurrentLesson="10"
        Lessons="@_lessons" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 10</span><h1>PLINQ - Parallel LINQ</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>30 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>AsParallel()</h2>
                    <pre><code class="language-csharp">// Sequential LINQ
var results = numbers
    .Where(n =&gt; n % 2 == 0)
    .Select(n =&gt; ExpensiveCalculation(n))
    .ToList();

// Parallel LINQ - just add AsParallel()!
var results = numbers
    .AsParallel()
    .Where(n =&gt; n % 2 == 0)
    .Select(n =&gt; ExpensiveCalculation(n))
    .ToList();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Controlling Parallelism</h2>
                    <pre><code class="language-csharp">var results = data
    .AsParallel()
    .WithDegreeOfParallelism(4)  // Max 4 threads
    .WithCancellation(cancellationToken)
    .WithExecutionMode(ParallelExecutionMode.ForceParallelism)
    .Select(x =&gt; Process(x))
    .ToList();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Preserving Order</h2>
                    <pre><code class="language-csharp">// Order NOT preserved by default (faster)
var unordered = data.AsParallel().Select(Process).ToList();

// Preserve order (slower)
var ordered = data
    .AsParallel()
    .AsOrdered()  // Maintain input order
    .Select(Process)
    .ToList();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>ForAll for Side Effects</h2>
                    <pre><code class="language-csharp">// Don't use ToList() just for side effects
data
    .AsParallel()
    .Where(x =&gt; x.IsActive)
    .ForAll(x =&gt;
    {
        // Process each in parallel (no results returned)
        ProcessItem(x);
    });</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>When PLINQ Helps</h2>
                    <div class="comparison-grid">
                        <div class="comparison-card">
                            <h4>Good for PLINQ</h4>
                            <ul>
                                <li>CPU-intensive transforms</li>
                                <li>Large datasets</li>
                                <li>Independent operations</li>
                            </ul>
                        </div>
                        <div class="comparison-card">
                            <h4>Avoid PLINQ</h4>
                            <ul>
                                <li>I/O operations</li>
                                <li>Small datasets</li>
                                <li>Operations with shared state</li>
                            </ul>
                        </div>
                    </div>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>AsParallel() converts LINQ to parallel.</LessonTakeaway>
                    <LessonTakeaway>Use AsOrdered() to preserve order.</LessonTakeaway>
                    <LessonTakeaway>ForAll() for parallel side effects.</LessonTakeaway>
                    <LessonTakeaway>Only use for CPU-bound, large datasets.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/async-concurrency/lesson/9" 
                PreviousText="Previous Lesson"
                NextUrl="/training/async-concurrency/lesson/11" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

@code {
    private readonly List<LessonSidebar.LessonInfo> _lessons =
    [
        new() { Number = 1, Title = "Introduction to Async", Url = "/training/async-concurrency/lesson/1" },
        new() { Number = 2, Title = "Tasks and Task<T>", Url = "/training/async-concurrency/lesson/2" },
        new() { Number = 3, Title = "Async/Await Keywords", Url = "/training/async-concurrency/lesson/3" },
        new() { Number = 4, Title = "Async Best Practices", Url = "/training/async-concurrency/lesson/4" },
        new() { Number = 5, Title = "Exception Handling", Url = "/training/async-concurrency/lesson/5" },
        new() { Number = 6, Title = "Cancellation Tokens", Url = "/training/async-concurrency/lesson/6" },
        new() { Number = 7, Title = "Progress Reporting", Url = "/training/async-concurrency/lesson/7" },
        new() { Number = 8, Title = "Parallel Programming", Url = "/training/async-concurrency/lesson/8" },
        new() { Number = 9, Title = "PLINQ", Url = "/training/async-concurrency/lesson/9" },
        new() { Number = 10, Title = "Concurrent Collections", Url = "/training/async-concurrency/lesson/10" },
        new() { Number = 11, Title = "Synchronization", Url = "/training/async-concurrency/lesson/11" },
        new() { Number = 12, Title = "Channels", Url = "/training/async-concurrency/lesson/12" },
        new() { Number = 13, Title = "Dataflow", Url = "/training/async-concurrency/lesson/13" },
        new() { Number = 14, Title = "ValueTask", Url = "/training/async-concurrency/lesson/14" },
        new() { Number = 15, Title = "Common Pitfalls", Url = "/training/async-concurrency/lesson/15" },
        new() { Number = 16, Title = "Debugging Async Code", Url = "/training/async-concurrency/lesson/16" }
    ];
}