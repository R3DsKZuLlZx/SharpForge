@page "/training/async-concurrency/lesson/5"
<PageTitle>Task Combinators - Async/Await & Concurrency - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <aside class="lesson-sidebar">
        <div class="sidebar-header"><a href="/training/async-concurrency" class="back-link"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>Course Overview</a><h3>Async/Await & Concurrency</h3></div>
        <nav class="lesson-nav"><a href="/training/async-concurrency/lesson/1" class="lesson-nav-item"><span class="lesson-num">1</span><span>Introduction</span></a><a href="/training/async-concurrency/lesson/2" class="lesson-nav-item"><span class="lesson-num">2</span><span>Tasks</span></a><a href="/training/async-concurrency/lesson/3" class="lesson-nav-item"><span class="lesson-num">3</span><span>Async/Await</span></a><a href="/training/async-concurrency/lesson/4" class="lesson-nav-item"><span class="lesson-num">4</span><span>ValueTask</span></a><a href="/training/async-concurrency/lesson/5" class="lesson-nav-item active"><span class="lesson-num">5</span><span>Combinators</span></a><a href="/training/async-concurrency/lesson/6" class="lesson-nav-item"><span class="lesson-num">6</span><span>Cancellation</span></a><a href="/training/async-concurrency/lesson/7" class="lesson-nav-item"><span class="lesson-num">7</span><span>Progress</span></a><a href="/training/async-concurrency/lesson/8" class="lesson-nav-item"><span class="lesson-num">8</span><span>Exceptions</span></a><a href="/training/async-concurrency/lesson/9" class="lesson-nav-item"><span class="lesson-num">9</span><span>Parallel.For</span></a><a href="/training/async-concurrency/lesson/10" class="lesson-nav-item"><span class="lesson-num">10</span><span>PLINQ</span></a><a href="/training/async-concurrency/lesson/11" class="lesson-nav-item"><span class="lesson-num">11</span><span>Dataflow</span></a><a href="/training/async-concurrency/lesson/12" class="lesson-nav-item"><span class="lesson-num">12</span><span>Sync</span></a><a href="/training/async-concurrency/lesson/13" class="lesson-nav-item"><span class="lesson-num">13</span><span>Collections</span></a><a href="/training/async-concurrency/lesson/14" class="lesson-nav-item"><span class="lesson-num">14</span><span>Channels</span></a><a href="/training/async-concurrency/lesson/15" class="lesson-nav-item"><span class="lesson-num">15</span><span>Pitfalls</span></a><a href="/training/async-concurrency/lesson/16" class="lesson-nav-item"><span class="lesson-num">16</span><span>Debugging</span></a></nav>
    </aside>
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 5</span><h1>Task Combinators</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>30 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Task.WhenAll</h2>
                    <p>Wait for all tasks to complete. Returns when ALL are done.</p>
                    <pre><code class="language-csharp">// Run multiple operations concurrently
var userTask = GetUserAsync(userId);
var ordersTask = GetOrdersAsync(userId);
var prefsTask = GetPreferencesAsync(userId);

// Wait for all to complete
await Task.WhenAll(userTask, ordersTask, prefsTask);

// Access results
var user = await userTask;
var orders = await ordersTask;
var prefs = await prefsTask;

// Or get all results at once
var urls = new[] { "url1", "url2", "url3" };
var tasks = urls.Select(url =&gt; httpClient.GetStringAsync(url));
string[] results = await Task.WhenAll(tasks);</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Task.WhenAny</h2>
                    <p>Returns when ANY task completes. Useful for timeouts and racing.</p>
                    <pre><code class="language-csharp">// Timeout pattern
var dataTask = GetDataAsync();
var timeoutTask = Task.Delay(TimeSpan.FromSeconds(5));

var completedTask = await Task.WhenAny(dataTask, timeoutTask);

if (completedTask == timeoutTask)
    throw new TimeoutException("Operation timed out");

var result = await dataTask;  // Get the actual result</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>First Successful Result</h2>
                    <pre><code class="language-csharp">// Try multiple sources, use first success
public async Task&lt;Data&gt; GetFromAnySourceAsync()
{
    var tasks = new List&lt;Task&lt;Data&gt;&gt;
    {
        GetFromCacheAsync(),
        GetFromDatabaseAsync(),
        GetFromApiAsync()
    };

    while (tasks.Count &gt; 0)
    {
        var completed = await Task.WhenAny(tasks);
        tasks.Remove(completed);

        try
        {
            return await completed;  // Return first success
        }
        catch
        {
            // Try next one
        }
    }

    throw new Exception("All sources failed");
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Throttling Concurrent Operations</h2>
                    <pre><code class="language-csharp">// Process with limited concurrency
public async Task ProcessAllAsync(IEnumerable&lt;string&gt; urls, int maxConcurrency = 10)
{
    var semaphore = new SemaphoreSlim(maxConcurrency);
    var tasks = urls.Select(async url =&gt;
    {
        await semaphore.WaitAsync();
        try
        {
            await ProcessUrlAsync(url);
        }
        finally
        {
            semaphore.Release();
        }
    });

    await Task.WhenAll(tasks);
}</code></pre>
                </section>

                <LessonTakeaways>
                        <LessonTakeaway>WhenAll waits for ALL tasks to complete.</LessonTakeaway>
                        <LessonTakeaway>WhenAny returns when ANY task completes.</LessonTakeaway>
                        <LessonTakeaway>Use SemaphoreSlim for throttling concurrency.</LessonTakeaway>
                    </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/async-concurrency/lesson/4" 
                PreviousText="Previous Lesson"
                NextUrl="/training/async-concurrency/lesson/6" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

