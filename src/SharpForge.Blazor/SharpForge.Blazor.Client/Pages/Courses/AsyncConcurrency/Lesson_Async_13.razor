@page "/training/async-concurrency/lesson/13"
<PageTitle>Concurrent Collections - Async/Await & Concurrency - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/async-concurrency"
        CourseTitle="Async/Await & Concurrency"
        CurrentLesson="13"
        Lessons="@_lessons" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 13</span><h1>Concurrent Collections</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>30 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>ConcurrentDictionary</h2>
                    <pre><code class="language-csharp">var cache = new ConcurrentDictionary&lt;string, Data&gt;();

// Thread-safe add or update
cache.TryAdd("key", new Data());
cache.AddOrUpdate("key", 
    addValue: new Data(),
    updateValueFactory: (key, existing) =&gt; new Data());

// Get or add atomically
var value = cache.GetOrAdd("key", key =&gt; LoadExpensiveData(key));

// Thread-safe read
if (cache.TryGetValue("key", out var data))
{
    // Use data
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>ConcurrentQueue and ConcurrentStack</h2>
                    <pre><code class="language-csharp">var queue = new ConcurrentQueue&lt;WorkItem&gt;();

// Producer
queue.Enqueue(new WorkItem());

// Consumer
if (queue.TryDequeue(out var item))
{
    ProcessItem(item);
}

// Stack (LIFO)
var stack = new ConcurrentStack&lt;WorkItem&gt;();
stack.Push(item);
stack.TryPop(out var popped);</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>ConcurrentBag</h2>
                    <pre><code class="language-csharp">// Thread-local storage optimized for add/take from same thread
var bag = new ConcurrentBag&lt;Result&gt;();

Parallel.ForEach(items, item =&gt;
{
    var result = Process(item);
    bag.Add(result);  // Fast - uses thread-local storage
});

// Get all results
var allResults = bag.ToArray();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>BlockingCollection</h2>
                    <pre><code class="language-csharp">using var collection = new BlockingCollection&lt;WorkItem&gt;(boundedCapacity: 100);

// Producer
Task.Run(() =&gt;
{
    foreach (var item in GetItems())
    {
        collection.Add(item);  // Blocks if full
    }
    collection.CompleteAdding();
});

// Consumer
foreach (var item in collection.GetConsumingEnumerable())
{
    ProcessItem(item);  // Blocks if empty
}</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>ConcurrentDictionary for thread-safe key-value storage.</LessonTakeaway>
                    <LessonTakeaway>ConcurrentQueue/Stack for producer-consumer.</LessonTakeaway>
                    <LessonTakeaway>ConcurrentBag optimized for same-thread add/take.</LessonTakeaway>
                    <LessonTakeaway>BlockingCollection for bounded producer-consumer.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/async-concurrency/lesson/12" 
                PreviousText="Previous Lesson"
                NextUrl="/training/async-concurrency/lesson/14" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

@code {
    private readonly List<LessonSidebar.LessonInfo> _lessons =
    [
        new() { Number = 1, Title = "Introduction to Async", Url = "/training/async-concurrency/lesson/1" },
        new() { Number = 2, Title = "Tasks and Task<T>", Url = "/training/async-concurrency/lesson/2" },
        new() { Number = 3, Title = "Async/Await Keywords", Url = "/training/async-concurrency/lesson/3" },
        new() { Number = 4, Title = "Async Best Practices", Url = "/training/async-concurrency/lesson/4" },
        new() { Number = 5, Title = "Exception Handling", Url = "/training/async-concurrency/lesson/5" },
        new() { Number = 6, Title = "Cancellation Tokens", Url = "/training/async-concurrency/lesson/6" },
        new() { Number = 7, Title = "Progress Reporting", Url = "/training/async-concurrency/lesson/7" },
        new() { Number = 8, Title = "Parallel Programming", Url = "/training/async-concurrency/lesson/8" },
        new() { Number = 9, Title = "PLINQ", Url = "/training/async-concurrency/lesson/9" },
        new() { Number = 10, Title = "Concurrent Collections", Url = "/training/async-concurrency/lesson/10" },
        new() { Number = 11, Title = "Synchronization", Url = "/training/async-concurrency/lesson/11" },
        new() { Number = 12, Title = "Channels", Url = "/training/async-concurrency/lesson/12" },
        new() { Number = 13, Title = "Dataflow", Url = "/training/async-concurrency/lesson/13" },
        new() { Number = 14, Title = "ValueTask", Url = "/training/async-concurrency/lesson/14" },
        new() { Number = 15, Title = "Common Pitfalls", Url = "/training/async-concurrency/lesson/15" },
        new() { Number = 16, Title = "Debugging Async Code", Url = "/training/async-concurrency/lesson/16" }
    ];
}