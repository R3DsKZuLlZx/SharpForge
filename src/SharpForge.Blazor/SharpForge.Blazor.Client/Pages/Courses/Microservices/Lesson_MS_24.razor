@page "/training/microservices/lesson/24"
<PageTitle>Distributed Tracing with OpenTelemetry - Microservices with .NET - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/microservices"
        CourseTitle="Microservices with .NET"
        CurrentLesson="24"
        Lessons="CourseLessons.Microservices" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 24</span><h1>Distributed Tracing with OpenTelemetry</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>30 min</span><span class="lesson-level advanced">Advanced</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>OpenTelemetry Setup</h2>
                    <pre><code class="language-csharp">// Program.cs
builder.Services.AddOpenTelemetry()
    .WithTracing(tracing =&gt;
    {
        tracing
            .AddAspNetCoreInstrumentation()
            .AddHttpClientInstrumentation()
            .AddSqlClientInstrumentation()
            .AddSource("OrderService")
            .AddOtlpExporter(opts =&gt;
            {
                opts.Endpoint = new Uri("http://jaeger:4317");
            });
    });</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Custom Spans</h2>
                    <pre><code class="language-csharp">public class OrderService
{
    private static readonly ActivitySource Source = new("OrderService");

    public async Task ProcessOrderAsync(Order order)
    {
        using var activity = Source.StartActivity("ProcessOrder");
        activity?.SetTag("order.id", order.Id.ToString());
        activity?.SetTag("order.total", order.Total);
        
        // Process order...
        
        activity?.AddEvent(new ActivityEvent("OrderValidated"));
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Trace Visualization</h2>
                    <pre><code class="language-text">Request Flow:
┌─────────────────────────────────────────────────────────────┐
│ API Gateway (12ms)                                          │
│   └─ Order Service (45ms)                                   │
│       ├─ Validate Order (5ms)                               │
│       ├─ Inventory Service (HTTP) (22ms)                    │
│       │   └─ Check Stock (15ms)                             │
│       └─ Payment Service (gRPC) (18ms)                      │
│           └─ Process Payment (12ms)                         │
└─────────────────────────────────────────────────────────────┘</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>OpenTelemetry is the standard for observability.</LessonTakeaway>
                    <LessonTakeaway>Traces show request flow across services.</LessonTakeaway>
                    <LessonTakeaway>Use Jaeger or Zipkin to visualize traces.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/microservices/lesson/23" 
                PreviousText="Previous Lesson"
                NextUrl="/training/microservices/lesson/25" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>