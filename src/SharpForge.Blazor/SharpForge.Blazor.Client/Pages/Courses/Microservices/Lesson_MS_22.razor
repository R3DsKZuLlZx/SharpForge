@page "/training/microservices/lesson/22"
<PageTitle>Scaling and Load Balancing - Microservices with .NET - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/microservices"
        CourseTitle="Microservices with .NET"
        CurrentLesson="22"
        Lessons="CourseLessons.Microservices" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 22</span><h1>Scaling and Load Balancing</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>25 min</span><span class="lesson-level advanced">Advanced</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Manual Scaling</h2>
                    <pre><code class="language-bash"># Scale deployment
kubectl scale deployment order-service --replicas=5

# Or update the YAML
spec:
  replicas: 5</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Horizontal Pod Autoscaler</h2>
                    <pre><code class="language-yaml">apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: order-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: order-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>KEDA (Event-Driven Autoscaling)</h2>
                    <pre><code class="language-yaml">apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: order-processor-scaler
spec:
  scaleTargetRef:
    name: order-processor
  minReplicaCount: 1
  maxReplicaCount: 20
  triggers:
  - type: rabbitmq
    metadata:
      queueName: orders
      queueLength: "50"</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>HPA scales based on CPU/memory metrics.</LessonTakeaway>
                    <LessonTakeaway>KEDA scales based on events (queues, etc).</LessonTakeaway>
                    <LessonTakeaway>Services load balance automatically.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/microservices/lesson/21" 
                PreviousText="Previous Lesson"
                NextUrl="/training/microservices/lesson/23" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>