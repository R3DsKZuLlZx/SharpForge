@using SharpForge.Blazor.Client.Data
@page "/training/design-patterns/lesson/10"
<PageTitle>Builder Pattern - Design Patterns in C# - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/design-patterns"
        CourseTitle="Design Patterns in C#"
        CurrentLesson="10"
        Lessons="CourseLessons.DesignPatterns" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 10</span><h1>Builder Pattern</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>30 min</span><span class="lesson-level advanced">Advanced</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Intent</h2>
                    <p>Separate the construction of a complex object from its representation, allowing the same construction process to create different representations.</p>
                </section>

                <section class="lesson-section">
                    <h2>Fluent Builder</h2>
                    <pre><code class="language-csharp">public class Email
{
    public string To { get; set; }
    public string From { get; set; }
    public string Subject { get; set; }
    public string Body { get; set; }
    public List&lt;string&gt; Attachments { get; set; } = new();
    public bool IsHtml { get; set; }
}

public class EmailBuilder
{
    private readonly Email _email = new();

    public EmailBuilder To(string to)
    {
        _email.To = to;
        return this;
    }

    public EmailBuilder From(string from)
    {
        _email.From = from;
        return this;
    }

    public EmailBuilder Subject(string subject)
    {
        _email.Subject = subject;
        return this;
    }

    public EmailBuilder Body(string body, bool isHtml = false)
    {
        _email.Body = body;
        _email.IsHtml = isHtml;
        return this;
    }

    public EmailBuilder WithAttachment(string path)
    {
        _email.Attachments.Add(path);
        return this;
    }

    public Email Build() =&gt; _email;
}

// Usage - fluent and readable!
var email = new EmailBuilder()
    .To("user@example.com")
    .From("noreply@company.com")
    .Subject("Welcome!")
    .Body("&lt;h1&gt;Hello&lt;/h1&gt;", isHtml: true)
    .WithAttachment("welcome.pdf")
    .Build();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Immutable Builder</h2>
                    <pre><code class="language-csharp">public class HttpRequest
{
    public string Url { get; }
    public string Method { get; }
    public Dictionary&lt;string, string&gt; Headers { get; }

    private HttpRequest(string url, string method, Dictionary&lt;string, string&gt; headers)
    {
        Url = url;
        Method = method;
        Headers = headers;
    }

    public class Builder
    {
        private string _url = "";
        private string _method = "GET";
        private readonly Dictionary&lt;string, string&gt; _headers = new();

        public Builder Url(string url) { _url = url; return this; }
        public Builder Method(string method) { _method = method; return this; }
        public Builder Header(string key, string value) { _headers[key] = value; return this; }

        public HttpRequest Build() =&gt; new(_url, _method, new Dictionary&lt;string, string&gt;(_headers));
    }
}

var request = new HttpRequest.Builder()
    .Url("https://api.example.com")
    .Method("POST")
    .Header("Authorization", "Bearer token")
    .Build();</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>Builder separates construction from representation.</LessonTakeaway>
                    <LessonTakeaway>Fluent interface makes code readable.</LessonTakeaway>
                    <LessonTakeaway>Great for objects with many optional parameters.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/design-patterns/lesson/9" 
                PreviousText="Previous Lesson"
                NextUrl="/training/design-patterns/lesson/11" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>