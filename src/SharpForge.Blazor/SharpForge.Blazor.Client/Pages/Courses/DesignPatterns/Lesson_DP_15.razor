@page "/training/design-patterns/lesson/15"
<PageTitle>Proxy Pattern - Design Patterns in C# - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/design-patterns"
        CourseTitle="Design Patterns in C#"
        CurrentLesson="15"
        Lessons="@_lessons" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 15</span><h1>Proxy Pattern</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>25 min</span><span class="lesson-level advanced">Advanced</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Intent</h2>
                    <p>Provide a surrogate or placeholder for another object to control access to it.</p>
                </section>

                <section class="lesson-section">
                    <h2>Types of Proxies</h2>
                    <ul>
                        <li><strong>Virtual Proxy:</strong> Lazy initialization of expensive objects</li>
                        <li><strong>Protection Proxy:</strong> Access control</li>
                        <li><strong>Caching Proxy:</strong> Cache results</li>
                        <li><strong>Logging Proxy:</strong> Add logging</li>
                    </ul>
                </section>

                <section class="lesson-section">
                    <h2>Caching Proxy</h2>
                    <pre><code class="language-csharp">public interface IWeatherService
{
    Task&lt;Weather&gt; GetWeatherAsync(string city);
}

public class WeatherService : IWeatherService
{
    public async Task&lt;Weather&gt; GetWeatherAsync(string city)
    {
        // Expensive API call
        await Task.Delay(1000);
        return new Weather { City = city, Temperature = 22 };
    }
}

public class CachingWeatherProxy : IWeatherService
{
    private readonly IWeatherService _service;
    private readonly Dictionary&lt;string, (Weather Data, DateTime Expiry)&gt; _cache = new();

    public CachingWeatherProxy(IWeatherService service) =&gt; _service = service;

    public async Task&lt;Weather&gt; GetWeatherAsync(string city)
    {
        if (_cache.TryGetValue(city, out var cached) &amp;&amp; cached.Expiry &gt; DateTime.Now)
            return cached.Data;

        var weather = await _service.GetWeatherAsync(city);
        _cache[city] = (weather, DateTime.Now.AddMinutes(10));
        return weather;
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Protection Proxy</h2>
                    <pre><code class="language-csharp">public class SecureDocumentProxy : IDocument
{
    private readonly IDocument _document;
    private readonly IUserContext _userContext;

    public SecureDocumentProxy(IDocument document, IUserContext userContext)
    {
        _document = document;
        _userContext = userContext;
    }

    public void Read() =&gt; _document.Read();  // Anyone can read

    public void Write(string content)
    {
        if (!_userContext.HasPermission("document:write"))
            throw new UnauthorizedAccessException();
        
        _document.Write(content);
    }

    public void Delete()
    {
        if (!_userContext.IsAdmin)
            throw new UnauthorizedAccessException();
        
        _document.Delete();
    }
}</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>Proxy controls access to another object.</LessonTakeaway>
                    <LessonTakeaway>Can add caching, security, logging, lazy loading.</LessonTakeaway>
                    <LessonTakeaway>Same interface as the real object.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/design-patterns/lesson/14" 
                PreviousText="Previous Lesson"
                NextUrl="/training/design-patterns/lesson/16" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

@code {
    private readonly List<LessonSidebar.LessonInfo> _lessons =
    [
        new() { Number = 1, Title = "Introduction to Patterns", Url = "/training/design-patterns/lesson/1" },
        new() { Number = 2, Title = "Singleton", Url = "/training/design-patterns/lesson/2" },
        new() { Number = 3, Title = "Factory Method", Url = "/training/design-patterns/lesson/3" },
        new() { Number = 4, Title = "Abstract Factory", Url = "/training/design-patterns/lesson/4" },
        new() { Number = 5, Title = "Builder", Url = "/training/design-patterns/lesson/5" },
        new() { Number = 6, Title = "Prototype", Url = "/training/design-patterns/lesson/6" },
        new() { Number = 7, Title = "Adapter", Url = "/training/design-patterns/lesson/7" },
        new() { Number = 8, Title = "Bridge", Url = "/training/design-patterns/lesson/8" },
        new() { Number = 9, Title = "Composite", Url = "/training/design-patterns/lesson/9" },
        new() { Number = 10, Title = "Decorator", Url = "/training/design-patterns/lesson/10" },
        new() { Number = 11, Title = "Facade", Url = "/training/design-patterns/lesson/11" },
        new() { Number = 12, Title = "Flyweight", Url = "/training/design-patterns/lesson/12" },
        new() { Number = 13, Title = "Proxy", Url = "/training/design-patterns/lesson/13" },
        new() { Number = 14, Title = "Chain of Responsibility", Url = "/training/design-patterns/lesson/14" },
        new() { Number = 15, Title = "Command", Url = "/training/design-patterns/lesson/15" },
        new() { Number = 16, Title = "Iterator", Url = "/training/design-patterns/lesson/16" },
        new() { Number = 17, Title = "Mediator", Url = "/training/design-patterns/lesson/17" },
        new() { Number = 18, Title = "Memento", Url = "/training/design-patterns/lesson/18" },
        new() { Number = 19, Title = "Observer", Url = "/training/design-patterns/lesson/19" },
        new() { Number = 20, Title = "State", Url = "/training/design-patterns/lesson/20" },
        new() { Number = 21, Title = "Strategy", Url = "/training/design-patterns/lesson/21" },
        new() { Number = 22, Title = "Template Method", Url = "/training/design-patterns/lesson/22" },
        new() { Number = 23, Title = "Visitor", Url = "/training/design-patterns/lesson/23" },
        new() { Number = 24, Title = "Patterns in Practice", Url = "/training/design-patterns/lesson/24" }
    ];
}