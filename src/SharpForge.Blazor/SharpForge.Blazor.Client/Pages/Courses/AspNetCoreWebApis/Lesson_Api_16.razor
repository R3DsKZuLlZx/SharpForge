@page "/training/aspnet-core-web-apis/lesson/16"
<PageTitle>Minimal APIs - ASP.NET Core Web APIs - SharpForge</PageTitle>
<CodeHighlighter />
<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/aspnet-core-web-apis"
        CourseTitle="ASP.NET Core Web APIs"
        CurrentLesson="16"
        Lessons="@_lessons" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 16</span><h1>Minimal APIs</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>35 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section"><h2>What are Minimal APIs?</h2><p>Minimal APIs provide a lightweight way to build HTTP APIs with minimal code. Perfect for microservices and simple endpoints.</p></section>
                <section class="lesson-section"><h2>Basic Endpoints</h2><pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.MapGet("/", () => "Hello World");
app.MapGet("/products", () => products);
app.MapGet("/products/{id}", (int id) => products.Find(p => p.Id == id));
app.MapPost("/products", (Product product) => { products.Add(product); return Results.Created($"/products/{product.Id}", product); });
app.MapPut("/products/{id}", (int id, Product product) => { /* update */ return Results.NoContent(); });
app.MapDelete("/products/{id}", (int id) => { /* delete */ return Results.NoContent(); });

app.Run();</code></pre></section>
                <section class="lesson-section"><h2>Dependency Injection</h2><pre><code class="language-csharp">app.MapGet("/products", async (IProductRepository repo) => 
    await repo.GetAllAsync());

app.MapGet("/products/{id}", async (int id, IProductRepository repo, ILogger&lt;Program&gt; logger) =>
{
    logger.LogInformation("Getting product {Id}", id);
    var product = await repo.GetByIdAsync(id);
    return product is null ? Results.NotFound() : Results.Ok(product);
});</code></pre></section>
                <section class="lesson-section"><h2>Route Groups</h2><pre><code class="language-csharp">var products = app.MapGroup("/api/products")
    .WithTags("Products")
    .RequireAuthorization();

products.MapGet("/", GetAll);
products.MapGet("/{id}", GetById);
products.MapPost("/", Create);
products.MapPut("/{id}", Update);
products.MapDelete("/{id}", Delete);</code></pre></section>
                <section class="lesson-section"><h2>Validation with Filters</h2><pre><code class="language-csharp">app.MapPost("/products", (Product product) => Results.Ok(product))
    .AddEndpointFilter(async (context, next) =>
    {
        var product = context.GetArgument&lt;Product&gt;(0);
        if (string.IsNullOrEmpty(product.Name))
            return Results.BadRequest("Name is required");
        return await next(context);
    });</code></pre></section>
                <LessonTakeaways>
                    <LessonTakeaway>Minimal APIs are perfect for simple services.</LessonTakeaway>
                    <LessonTakeaway>Use route groups to organize related endpoints.</LessonTakeaway>
                    <LessonTakeaway>Endpoint filters provide validation and cross-cutting concerns.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/aspnet-core-web-apis/lesson/15" 
                PreviousText="Previous Lesson"
                NextUrl="/training/aspnet-core-web-apis/lesson/17" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

@code {
    private readonly List<LessonSidebar.LessonInfo> _lessons =
    [
        new() { Number = 1, Title = "Introduction to Web APIs", Url = "/training/aspnet-core-web-apis/lesson/1" },
        new() { Number = 2, Title = "Creating Your First API", Url = "/training/aspnet-core-web-apis/lesson/2" },
        new() { Number = 3, Title = "Routing and Endpoints", Url = "/training/aspnet-core-web-apis/lesson/3" },
        new() { Number = 4, Title = "Model Binding", Url = "/training/aspnet-core-web-apis/lesson/4" },
        new() { Number = 5, Title = "Validation", Url = "/training/aspnet-core-web-apis/lesson/5" },
        new() { Number = 6, Title = "Dependency Injection", Url = "/training/aspnet-core-web-apis/lesson/6" },
        new() { Number = 7, Title = "Entity Framework Core", Url = "/training/aspnet-core-web-apis/lesson/7" },
        new() { Number = 8, Title = "Repository Pattern", Url = "/training/aspnet-core-web-apis/lesson/8" },
        new() { Number = 9, Title = "Error Handling", Url = "/training/aspnet-core-web-apis/lesson/9" },
        new() { Number = 10, Title = "Authentication", Url = "/training/aspnet-core-web-apis/lesson/10" },
        new() { Number = 11, Title = "Authorization", Url = "/training/aspnet-core-web-apis/lesson/11" },
        new() { Number = 12, Title = "API Versioning", Url = "/training/aspnet-core-web-apis/lesson/12" },
        new() { Number = 13, Title = "Caching", Url = "/training/aspnet-core-web-apis/lesson/13" },
        new() { Number = 14, Title = "Rate Limiting", Url = "/training/aspnet-core-web-apis/lesson/14" },
        new() { Number = 15, Title = "OpenAPI/Swagger", Url = "/training/aspnet-core-web-apis/lesson/15" },
        new() { Number = 16, Title = "Testing APIs", Url = "/training/aspnet-core-web-apis/lesson/16" },
        new() { Number = 17, Title = "Logging and Monitoring", Url = "/training/aspnet-core-web-apis/lesson/17" },
        new() { Number = 18, Title = "Deployment", Url = "/training/aspnet-core-web-apis/lesson/18" }
    ];
}