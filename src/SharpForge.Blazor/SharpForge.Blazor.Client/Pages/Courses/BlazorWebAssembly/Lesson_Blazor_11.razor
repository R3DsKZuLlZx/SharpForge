@page "/training/blazor-webassembly/lesson/11"
<PageTitle>State Management - Blazor WebAssembly - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="/training/blazor-webassembly"
        CourseTitle="Blazor WebAssembly"
        CurrentLesson="11"
        Lessons="@_lessons" />
    <main class="lesson-content">
        <article class="lesson-article">
            <header class="lesson-header"><span class="lesson-number">Lesson 11</span><h1>State Management</h1><div class="lesson-meta"><span class="lesson-duration"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>35 min</span><span class="lesson-level intermediate">Intermediate</span></div></header>
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>State Management Options</h2>
                    <ul>
                        <li><strong>Component state:</strong> Local to a single component</li>
                        <li><strong>Cascading values:</strong> Pass state down the component tree</li>
                        <li><strong>Scoped services:</strong> Share state via DI</li>
                        <li><strong>Browser storage:</strong> Persist state in localStorage/sessionStorage</li>
                    </ul>
                </section>

                <section class="lesson-section">
                    <h2>State Container Service</h2>
                    <pre><code class="language-csharp">// Services/AppState.cs
public class AppState
{
    public string? CurrentUser { get; private set; }
    public List&lt;CartItem&gt; CartItems { get; } = new();

    public event Action? OnChange;

    public void SetUser(string user)
    {
        CurrentUser = user;
        NotifyStateChanged();
    }

    public void AddToCart(Product product)
    {
        CartItems.Add(new CartItem { Product = product, Quantity = 1 });
        NotifyStateChanged();
    }

    private void NotifyStateChanged() =&gt; OnChange?.Invoke();
}

// Register as singleton (persists for app lifetime)
builder.Services.AddSingleton&lt;AppState&gt;();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Using State in Components</h2>
                    <pre><code class="language-razor">@@inject AppState State
@@implements IDisposable

&lt;p&gt;Cart has @@State.CartItems.Count items&lt;/p&gt;

@@code {
    protected override void OnInitialized()
    {
        State.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        State.OnChange -= StateHasChanged;
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Browser Storage</h2>
                    <pre><code class="language-bash">dotnet add package Blazored.LocalStorage</code></pre>
                    <pre><code class="language-csharp">// Program.cs
builder.Services.AddBlazoredLocalStorage();</code></pre>
                    <pre><code class="language-razor">@@inject Blazored.LocalStorage.ILocalStorageService LocalStorage

@@code {
    protected override async Task OnInitializedAsync()
    {
        // Load from storage
        var theme = await LocalStorage.GetItemAsync&lt;string&gt;("theme");
    }

    private async Task SaveTheme(string theme)
    {
        await LocalStorage.SetItemAsync("theme", theme);
    }

    private async Task ClearTheme()
    {
        await LocalStorage.RemoveItemAsync("theme");
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Cascading State</h2>
                    <pre><code class="language-razor">@@* App.razor or MainLayout.razor *@@
&lt;CascadingValue Value="@@appState"&gt;
    @@Body
&lt;/CascadingValue&gt;

@@code {
    private AppState appState = new();
}

@@* Any child component *@@
@@code {
    [CascadingParameter]
    public AppState? State { get; set; }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>URL-Based State</h2>
                    <pre><code class="language-razor">@@page "/products"
@@inject NavigationManager Nav

@@* State in query string *@@
@@code {
    [SupplyParameterFromQuery]
    public string? Category { get; set; }

    [SupplyParameterFromQuery]
    public int Page { get; set; } = 1;

    private void ChangePage(int page)
    {
        Nav.NavigateTo($"/products?category={Category}&amp;page={page}");
    }
}</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>Use singleton services for app-wide state.</LessonTakeaway>
                    <LessonTakeaway>Event pattern (OnChange) notifies components of updates.</LessonTakeaway>
                    <LessonTakeaway>Use localStorage for persistent client-side state.</LessonTakeaway>
                    <LessonTakeaway>Query strings make state shareable via URL.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="/training/blazor-webassembly/lesson/10" 
                PreviousText="Previous Lesson"
                NextUrl="/training/blazor-webassembly/lesson/12" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>

@code {
    private readonly List<LessonSidebar.LessonInfo> _lessons =
    [
        new() { Number = 1, Title = "Introduction to Blazor", Url = "/training/blazor-webassembly/lesson/1" },
        new() { Number = 2, Title = "Components Basics", Url = "/training/blazor-webassembly/lesson/2" },
        new() { Number = 3, Title = "Data Binding", Url = "/training/blazor-webassembly/lesson/3" },
        new() { Number = 4, Title = "Event Handling", Url = "/training/blazor-webassembly/lesson/4" },
        new() { Number = 5, Title = "Component Parameters", Url = "/training/blazor-webassembly/lesson/5" },
        new() { Number = 6, Title = "Layouts and Routing", Url = "/training/blazor-webassembly/lesson/6" },
        new() { Number = 7, Title = "Forms and Validation", Url = "/training/blazor-webassembly/lesson/7" },
        new() { Number = 8, Title = "Dependency Injection", Url = "/training/blazor-webassembly/lesson/8" },
        new() { Number = 9, Title = "HTTP and API Calls", Url = "/training/blazor-webassembly/lesson/9" },
        new() { Number = 10, Title = "State Management", Url = "/training/blazor-webassembly/lesson/10" },
        new() { Number = 11, Title = "JavaScript Interop", Url = "/training/blazor-webassembly/lesson/11" },
        new() { Number = 12, Title = "Authentication", Url = "/training/blazor-webassembly/lesson/12" },
        new() { Number = 13, Title = "Performance", Url = "/training/blazor-webassembly/lesson/13" },
        new() { Number = 14, Title = "Deployment", Url = "/training/blazor-webassembly/lesson/14" }
    ];
}