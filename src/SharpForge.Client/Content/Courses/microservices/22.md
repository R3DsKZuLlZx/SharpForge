---
title: "Scaling"
courseSlug: "microservices"
lessonNumber: 22
duration: "12 min"
takeaways:
  - "HPA scales based on CPU/memory metrics."
  - "KEDA scales based on events (queues, etc)."
  - "Services load balance automatically."
---

## Manual Scaling

Simple kubectl command to scale a deployment and the corresponding YAML replicas field to update desired replica count.

```bash
# Scale deployment
kubectl scale deployment order-service --replicas=5

# Or update the YAML
spec:
  replicas: 5
```

## Horizontal Pod Autoscaler

HPA manifest scales pods based on metrics such as CPU utilization - set minimum/maximum replicas and target metrics.

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: order-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: order-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
```

## KEDA (Event-Driven Autoscaling)

KEDA enables event-driven autoscaling (e.g., scale based on queue length) - useful for workloads driven by messaging systems.

```yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: order-processor-scaler
spec:
  scaleTargetRef:
    name: order-processor
  minReplicaCount: 1
  maxReplicaCount: 20
  triggers:
  - type: rabbitmq
    metadata:
      queueName: orders
      queueLength: "50"
```

