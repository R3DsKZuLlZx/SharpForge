---
title: "Parallel.For and Parallel.ForEach"
courseSlug: "async-concurrency"
lessonNumber: 9
duration: "15 min"
takeaways:
  - "Parallel loops are for CPU-bound work."
  - "Use MaxDegreeOfParallelism to control threads."
  - "Parallel.ForEachAsync combines parallel + async."
---

## When to Use Parallel Loops

Use Parallel loops for **CPU-bound** work that can be distributed across cores.

<div class="info-box">
    <div class="info-box-header"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg><span>Key Point</span></div>
    <p>For I/O-bound work, use async/await with Task.WhenAll instead.</p>
</div>

## Parallel.For

Parallel.For executes iterations in parallel across multiple threads. Use for CPU-bound work and avoid shared mutable state unless synchronized.

```csharp
// Sequential
for (int i = 0; i < 1000; i++)
{
    ProcessItem(i);
}

// Parallel - uses multiple cores
Parallel.For(0, 1000, i =>
{
    ProcessItem(i);  // Each iteration may run on different thread
});
```

## Parallel.ForEach

Parallel.ForEach processes collections in parallel; ensure thread-safety for any shared resources when using parallel loops.

```csharp
var files = Directory.GetFiles(path, "*.jpg");

// Process all files in parallel
Parallel.ForEach(files, file =>
{
    var image = LoadImage(file);
    var thumbnail = CreateThumbnail(image);
    SaveThumbnail(thumbnail, file);
});
```

## ParallelOptions

Use `ParallelOptions` to set max degree of parallelism and cancellation support for parallel loops.

```csharp
var options = new ParallelOptions
{
    MaxDegreeOfParallelism = Environment.ProcessorCount,
    CancellationToken = cancellationToken
};

try
{
    Parallel.ForEach(items, options, item =>
    {
        ProcessItem(item);
    });
}
catch (OperationCanceledException)
{
    Console.WriteLine("Operation was cancelled");
}
```

## Parallel.ForEachAsync (.NET 6+)

Use `Parallel.ForEachAsync` when you need to perform asynchronous operations with controlled concurrency in .NET 6 and later.

```csharp
// For async operations with controlled concurrency
await Parallel.ForEachAsync(urls, 
    new ParallelOptions { MaxDegreeOfParallelism = 10 },
    async (url, ct) =>
    {
        var content = await httpClient.GetStringAsync(url, ct);
        await ProcessContentAsync(content);
    });
```

