---
title: "Project Setup"
courseSlug: "blazor-webassembly"
lessonNumber: 2
duration: "10 min"
takeaways:
  - "Use dotnet new blazorwasm to create a new project."
  - "Program.cs configures services and root components."
  - "App.razor handles routing for your application."
  - "wwwroot contains static files like CSS and images."
---

## Creating a Blazor WebAssembly Project

```bash
# Standalone Blazor WebAssembly
dotnet new blazorwasm -n MyBlazorApp

# Blazor WebAssembly with ASP.NET Core hosted
dotnet new blazorwasm -n MyBlazorApp --hosted

# Run the app
cd MyBlazorApp
dotnet run
```

## Project Structure

```bash
MyBlazorApp/
├── Properties/
│   └── launchSettings.json
├── wwwroot/
│   ├── css/
│   ├── index.html          # Host page
│   └── favicon.png
├── Pages/
│   ├── Counter.razor
│   ├── Home.razor
│   └── Weather.razor
├── Layout/
│   ├── MainLayout.razor    # Main layout
│   └── NavMenu.razor       # Navigation
├── _Imports.razor          # Global usings
├── App.razor               # Root component
├── Program.cs              # Entry point
└── MyBlazorApp.csproj
```

## Program.cs - Entry Point

This snippet shows the Blazor WebAssembly application bootstrap. It registers the root components and configures services such as `HttpClient` with a base address. Use this file to wire up DI and application startup behavior.

```csharp
using Microsoft.AspNetCore.Components.Web;
using Microsoft.AspNetCore.Components.WebAssembly.Hosting;
using MyBlazorApp;

var builder = WebAssemblyHostBuilder.CreateDefault(args);
builder.RootComponents.Add<App>("#app");
builder.RootComponents.Add<HeadOutlet>("head::after");

builder.Services.AddScoped(sp => 
    new HttpClient { BaseAddress = new Uri(builder.HostEnvironment.BaseAddress) });

await builder.Build().RunAsync();
```

## index.html - Host Page

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>MyBlazorApp</title>
    <base href="/" />
    <link href="css/app.css" rel="stylesheet" />
</head>
<body>
    <div id="app">Loading...</div>

    <script src="_framework/blazor.webassembly.js"></script>
</body>
</html>
```

## App.razor - Root Component

```razor
<Router AppAssembly="@@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@@routeData" DefaultLayout="@@typeof(MainLayout)" />
        <FocusOnNavigate RouteData="@@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@@typeof(MainLayout)">
            <p>Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
```

