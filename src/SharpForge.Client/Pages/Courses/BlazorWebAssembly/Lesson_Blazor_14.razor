@page "/training/blazor-webassembly/lesson/14"
<PageTitle>Deployment - Blazor WebAssembly - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="training/blazor-webassembly"
        CourseTitle="Blazor WebAssembly"
        CurrentLesson="14"
        Lessons="CourseLessons.BlazorWebAssembly" />
    <main class="lesson-content">
        <article class="lesson-article">
            <LessonHeader 
                LessonNumber="14"
                Title="Deployment"
                Duration="30 min"
                Level="Intermediate" />
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Publishing for Production</h2>
                    <pre><code class="language-bash"># Publish with Release configuration
dotnet publish -c Release -o publish

# Output is in publish/wwwroot/
# These are static files ready to deploy!</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Optimizing Bundle Size</h2>
                    <pre><code class="language-xml">&lt;!-- In .csproj --&gt;
&lt;PropertyGroup&gt;
  &lt;!-- Enable trimming to reduce size --&gt;
  &lt;PublishTrimmed&gt;true&lt;/PublishTrimmed&gt;
  
  &lt;!-- Enable AOT for faster startup (larger download) --&gt;
  &lt;RunAOTCompilation&gt;true&lt;/RunAOTCompilation&gt;
  
  &lt;!-- Compress with Brotli --&gt;
  &lt;BlazorEnableCompression&gt;true&lt;/BlazorEnableCompression&gt;
&lt;/PropertyGroup&gt;</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Azure Static Web Apps</h2>
                    <pre><code class="language-yaml"># .github/workflows/azure-static-web-apps.yml
name: Deploy to Azure Static Web Apps

on:
  push:
    branches: [main]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      
      - name: Publish
        run: dotnet publish -c Release -o publish
      
      - name: Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_TOKEN }}
          action: "upload"
          app_location: "publish/wwwroot"</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>GitHub Pages</h2>
                    <pre><code class="language-xml">&lt;!-- Set base path in index.html --&gt;
&lt;base href="/your-repo-name/" /&gt;</code></pre>
                    <pre><code class="language-yaml"># .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      
      - run: dotnet publish -c Release -o release
      
      # Add .nojekyll to bypass Jekyll processing
      - run: touch release/wwwroot/.nojekyll
      
      # Handle SPA routing
      - run: cp release/wwwroot/index.html release/wwwroot/404.html
      
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: release/wwwroot</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Docker Deployment</h2>
                    <pre><code class="language-dockerfile"># Dockerfile
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY . .
RUN dotnet publish -c Release -o /app/publish

FROM nginx:alpine
COPY --from=build /app/publish/wwwroot /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80</code></pre>
                    <pre><code class="language-nginx"># nginx.conf - Handle SPA routing
events { }
http {
    include mime.types;
    server {
        listen 80;
        root /usr/share/nginx/html;
        index index.html;
        
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>PWA Support</h2>
                    <pre><code class="language-bash"># Create with PWA template
dotnet new blazorwasm -n MyApp --pwa</code></pre>
                    <pre><code class="language-json">// wwwroot/manifest.json
{
  "name": "My Blazor App",
  "short_name": "BlazorApp",
  "start_url": "./",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#512bd4",
  "icons": [
    {
      "src": "icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>Blazor WASM publishes to static files—host anywhere!</LessonTakeaway>
                    <LessonTakeaway>Enable trimming and compression to reduce size.</LessonTakeaway>
                    <LessonTakeaway>Configure server for SPA routing (404 → index.html).</LessonTakeaway>
                    <LessonTakeaway>Add PWA support for installable offline apps.</LessonTakeaway>
                </LessonTakeaways>

                <LessonCongratulations 
                    CourseTitle="Blazor WebAssembly"
                    Topics="@(new List<string> {
                        "Creating and structuring Blazor WebAssembly projects",
                        "Building reusable Razor components",
                        "Component parameters, events, and data binding",
                        "Component lifecycle and state management",
                        "Routing, forms, and validation",
                        "Calling REST APIs with HttpClient",
                        "JavaScript interoperability",
                        "Authentication and authorization",
                        "Deployment to Azure, GitHub Pages, and Docker"
                    })"
                    ClosingMessage="<strong>What's next?</strong> You're now ready to build full-featured web applications with Blazor WebAssembly!" />
            </div>
            <LessonFooter 
                PreviousUrl="training/blazor-webassembly/lesson/13" 
                PreviousText="Previous Lesson"
                NextUrl="training/blazor-webassembly" 
                NextText="Back to Course"
                ShowHomeIcon="true" />
        </article>
    </main>
</div>
