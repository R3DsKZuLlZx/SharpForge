@page "/training/getting-started-dotnet/lesson/8"

<PageTitle>Introduction to ASP.NET Core - Getting Started with .NET - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="training/getting-started-dotnet"
        CourseTitle="Getting Started with .NET"
        CurrentLesson="8"
        Lessons="CourseLessons.GettingStartedDotNet" />
    <main class="lesson-content">
        <article class="lesson-article">
            <LessonHeader 
                LessonNumber="8"
                Title="Introduction to ASP.NET Core"
                Duration="15 min"
                Level="Beginner" />

            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>What is ASP.NET Core?</h2>
                    <p>ASP.NET Core is a cross-platform, high-performance framework for building modern web applications and APIs. It's the web development framework for .NET, enabling you to build everything from REST APIs to full-stack web applications.</p>
                </section>

                <section class="lesson-section">
                    <h2>Web Project Types</h2>
                    <div class="project-types">
                        <div class="project-type-card">
                            <h4>Web API</h4>
                            <p>REST APIs that return JSON/XML data. Perfect for backends and microservices.</p>
                            <code>dotnet new webapi</code>
                        </div>
                        <div class="project-type-card">
                            <h4>MVC</h4>
                            <p>Model-View-Controller pattern for server-rendered HTML pages.</p>
                            <code>dotnet new mvc</code>
                        </div>
                        <div class="project-type-card">
                            <h4>Razor Pages</h4>
                            <p>Page-focused framework for simpler web UIs.</p>
                            <code>dotnet new webapp</code>
                        </div>
                        <div class="project-type-card">
                            <h4>Blazor</h4>
                            <p>Interactive web UIs using C# instead of JavaScript.</p>
                            <code>dotnet new blazorwasm</code>
                        </div>
                    </div>
                </section>

                <section class="lesson-section">
                    <h2>Creating Your First API</h2>
                    <pre><code class="language-bash"># Create a minimal API project
dotnet new web -n MyFirstApi
cd MyFirstApi
dotnet run</code></pre>

                    <p>The generated <code>Program.cs</code>:</p>

                    <pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.MapGet("/", () => "Hello World!");

app.Run();</code></pre>

                    <p>Visit <code>http://localhost:5000</code> to see your API running!</p>
                </section>

                <section class="lesson-section">
                    <h2>The Application Pipeline</h2>
                    <p>ASP.NET Core uses a pipeline of middleware components to handle requests:</p>

                    <div class="pipeline-diagram">
                        <div class="pipeline-step">Request</div>
                        <div class="pipeline-arrow">→</div>
                        <div class="pipeline-step">Middleware 1</div>
                        <div class="pipeline-arrow">→</div>
                        <div class="pipeline-step">Middleware 2</div>
                        <div class="pipeline-arrow">→</div>
                        <div class="pipeline-step">Endpoint</div>
                        <div class="pipeline-arrow">→</div>
                        <div class="pipeline-step">Response</div>
                    </div>

                    <pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

// Middleware order matters!
app.UseHttpsRedirection();  // Redirect HTTP to HTTPS
app.UseStaticFiles();       // Serve static files
app.UseRouting();           // Enable routing
app.UseAuthentication();    // Who is the user?
app.UseAuthorization();     // Can they access this?

app.MapGet("/", () => "Hello World!");

app.Run();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Minimal APIs</h2>
                    <p>Minimal APIs provide a lightweight way to build HTTP APIs:</p>

                    <pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

// Simple GET endpoint
app.MapGet("/hello", () => "Hello, World!");

// With route parameters
app.MapGet("/hello/{name}", (string name) => $"Hello, {name}!");

// POST endpoint with body
app.MapPost("/users", (User user) => 
{
    // Create user...
    return Results.Created($"/users/{user.Id}", user);
});

// With query parameters
app.MapGet("/search", (string? query, int page = 1) => 
    $"Searching for: {query}, page {page}");

// Return different status codes
app.MapGet("/users/{id}", (int id) => 
{
    var user = GetUser(id);
    return user is not null 
        ? Results.Ok(user) 
        : Results.NotFound();
});

app.Run();

record User(int Id, string Name, string Email);</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Dependency Injection</h2>
                    <p>ASP.NET Core has built-in dependency injection:</p>

                    <pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);

// Register services
builder.Services.AddScoped&lt;IUserService, UserService&gt;();
builder.Services.AddSingleton&lt;ICache, MemoryCache&gt;();

var app = builder.Build();

// Inject into endpoints
app.MapGet("/users", (IUserService userService) => 
    userService.GetAllUsers());

app.MapGet("/users/{id}", async (int id, IUserService userService) => 
{
    var user = await userService.GetByIdAsync(id);
    return user is not null ? Results.Ok(user) : Results.NotFound();
});

app.Run();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Controller-Based APIs</h2>
                    <p>For larger APIs, controllers provide better organization:</p>

                    <pre><code class="language-bash">dotnet new webapi -n MyApi --use-controllers</code></pre>

                    <pre><code class="language-csharp">// Controllers/UsersController.cs
using Microsoft.AspNetCore.Mvc;

[ApiController]
[Route("api/[controller]")]
public class UsersController : ControllerBase
{
    private readonly IUserService _userService;
    
    public UsersController(IUserService userService)
    {
        _userService = userService;
    }
    
    [HttpGet]
    public async Task&lt;ActionResult&lt;IEnumerable&lt;User&gt;&gt;&gt; GetAll()
    {
        return Ok(await _userService.GetAllAsync());
    }
    
    [HttpGet("{id}")]
    public async Task&lt;ActionResult&lt;User&gt;&gt; GetById(int id)
    {
        var user = await _userService.GetByIdAsync(id);
        if (user == null) return NotFound();
        return Ok(user);
    }
    
    [HttpPost]
    public async Task&lt;ActionResult&lt;User&gt;&gt; Create(CreateUserDto dto)
    {
        var user = await _userService.CreateAsync(dto);
        return CreatedAtAction(nameof(GetById), new { id = user.Id }, user);
    }
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Configuration in Web Apps</h2>
                    <p>ASP.NET Core automatically loads configuration:</p>

                    <pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);

// Configuration is already available
var connectionString = builder.Configuration.GetConnectionString("Default");

// Use options pattern
builder.Services.Configure&lt;MySettings&gt;(
    builder.Configuration.GetSection("MySettings"));

var app = builder.Build();

// Access configuration in endpoints
app.MapGet("/config", (IConfiguration config) => 
    config["MySettings:ApiKey"]);

app.Run();</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Running and Testing</h2>
                    <pre><code class="language-bash"># Run the application
dotnet run

# Run with hot reload
dotnet watch run

# Run on specific port
dotnet run --urls "http://localhost:3000"</code></pre>

                    <p>Test with curl or a tool like Postman:</p>

                    <pre><code class="language-bash"># GET request
curl http://localhost:5000/hello

# POST request with JSON body
curl -X POST http://localhost:5000/users \
  -H "Content-Type: application/json" \
  -d '{"name": "Alice", "email": "alice@example.com"}'</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>OpenAPI / Swagger</h2>
                    <p>Add API documentation with Swagger:</p>

                    <pre><code class="language-csharp">var builder = WebApplication.CreateBuilder(args);

builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

var app = builder.Build();

if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.MapGet("/hello", () => "Hello World!")
    .WithName("GetHello")
    .WithOpenApi();

app.Run();</code></pre>

                    <p>Visit <code>/swagger</code> to see interactive API documentation!</p>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>ASP.NET Core is a cross-platform, high-performance web framework.</LessonTakeaway>
                    <LessonTakeaway>Use Minimal APIs for simple endpoints, Controllers for larger APIs.</LessonTakeaway>
                    <LessonTakeaway>Dependency injection is built-in and configured in Program.cs.</LessonTakeaway>
                    <LessonTakeaway>Use Swagger for API documentation during development.</LessonTakeaway>
                </LessonTakeaways>
            </div>

            <LessonFooter 
                PreviousUrl="training/getting-started-dotnet/lesson/7" 
                PreviousText="Previous Lesson"
                NextUrl="training/getting-started-dotnet/lesson/9" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>
