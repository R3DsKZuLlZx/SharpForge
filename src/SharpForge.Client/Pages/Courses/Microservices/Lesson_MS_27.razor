@page "/training/microservices/lesson/27"
<PageTitle>Polly for .NET - Microservices with .NET - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="training/microservices"
        CourseTitle="Microservices with .NET"
        CurrentLesson="27"
        Lessons="CourseLessons.Microservices" />
    <main class="lesson-content">
        <article class="lesson-article">
            <LessonHeader 
                LessonNumber="27"
                Title="Polly for .NET"
                Duration="30 min"
                Level="Advanced" />
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>.NET 8+ Resilience (Polly v8)</h2>
                    <p>Example showing how to add the built-in standard resilience handler to an HttpClient for retries, circuit breaker and timeouts.</p>
                    <pre><code class="language-csharp">// Program.cs
builder.Services.AddHttpClient&lt;IInventoryClient, InventoryClient&gt;()
    .AddStandardResilienceHandler();  // Retry + CB + Timeout built-in!</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Custom Resilience Pipeline</h2>
                    <p>Configure a custom pipeline with retry, circuit breaker and timeout parameters for a specific client.</p>
                    <pre><code class="language-csharp">builder.Services.AddHttpClient&lt;IOrderClient, OrderClient&gt;()
    .AddResilienceHandler("orders", builder =&gt;
    {
        builder
            .AddRetry(new HttpRetryStrategyOptions
            {
                MaxRetryAttempts = 3,
                Delay = TimeSpan.FromSeconds(1),
                BackoffType = DelayBackoffType.Exponential,
                UseJitter = true
            })
            .AddCircuitBreaker(new HttpCircuitBreakerStrategyOptions
            {
                SamplingDuration = TimeSpan.FromSeconds(30),
                FailureRatio = 0.5,
                MinimumThroughput = 10,
                BreakDuration = TimeSpan.FromSeconds(30)
            })
            .AddTimeout(TimeSpan.FromSeconds(10));
    });</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Fallback</h2>
                    <p>Fallback strategy returns a cached or default response if all resilience steps fail.</p>
                    <pre><code class="language-csharp">var pipeline = new ResiliencePipelineBuilder&lt;HttpResponseMessage&gt;()
    .AddFallback(new FallbackStrategyOptions&lt;HttpResponseMessage&gt;
    {
        FallbackAction = args =&gt;
        {
            // Return cached/default response
            var response = new HttpResponseMessage(HttpStatusCode.OK)
            {
                Content = JsonContent.Create(new { cached = true })
            };
            return Outcome.FromResultAsValueTask(response);
        }
    })
    .Build();</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>Use AddStandardResilienceHandler for quick setup.</LessonTakeaway>
                    <LessonTakeaway>Combine retry, circuit breaker, and timeout.</LessonTakeaway>
                    <LessonTakeaway>Fallback provides graceful degradation.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="training/microservices/lesson/26" 
                PreviousText="Previous Lesson"
                NextUrl="training/microservices/lesson/28" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>
