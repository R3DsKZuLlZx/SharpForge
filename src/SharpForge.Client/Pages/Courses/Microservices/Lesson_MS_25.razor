@page "/training/microservices/lesson/25"
<PageTitle>Metrics and Monitoring - Microservices with .NET - SharpForge</PageTitle>
<CodeHighlighter />

<div class="lesson-layout">
    <LessonSidebar 
        CourseUrl="training/microservices"
        CourseTitle="Microservices with .NET"
        CurrentLesson="25"
        Lessons="CourseLessons.Microservices" />
    <main class="lesson-content">
        <article class="lesson-article">
            <LessonHeader 
                LessonNumber="25"
                Title="Metrics and Monitoring"
                Duration="30 min"
                Level="Advanced" />
            <div class="lesson-body">
                <section class="lesson-section">
                    <h2>Prometheus Metrics</h2>
                    <p>Example registering OpenTelemetry metrics and exposing the /metrics endpoint for Prometheus to scrape.</p>
                    <pre><code class="language-csharp">// Program.cs
builder.Services.AddOpenTelemetry()
    .WithMetrics(metrics =&gt;
    {
        metrics
            .AddAspNetCoreInstrumentation()
            .AddHttpClientInstrumentation()
            .AddRuntimeInstrumentation()
            .AddPrometheusExporter();
    });

app.MapPrometheusScrapingEndpoint();  // /metrics</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Custom Metrics</h2>
                    <p>Example of defining counters and histograms for domain-specific metrics such as orders placed and processing time.</p>
                    <pre><code class="language-csharp">public class OrderMetrics
{
    private readonly Counter&lt;int&gt; _ordersPlaced;
    private readonly Histogram&lt;double&gt; _orderProcessingTime;

    public OrderMetrics(IMeterFactory meterFactory)
    {
        var meter = meterFactory.Create("OrderService");
        
        _ordersPlaced = meter.CreateCounter&lt;int&gt;(
            "orders_placed_total",
            description: "Total orders placed");
            
        _orderProcessingTime = meter.CreateHistogram&lt;double&gt;(
            "order_processing_seconds",
            description: "Order processing time");
    }

    public void OrderPlaced() =&gt; _ordersPlaced.Add(1);
    public void RecordProcessingTime(double seconds) =&gt; 
        _orderProcessingTime.Record(seconds);
}</code></pre>
                </section>

                <section class="lesson-section">
                    <h2>Grafana Dashboard</h2>
                    <pre><code class="language-text">Key Metrics to Monitor:
- Request rate (requests/sec)
- Error rate (% of 5xx responses)
- Latency (p50, p95, p99)
- CPU and Memory usage
- Database connection pool
- Message queue depth</code></pre>
                </section>

                <LessonTakeaways>
                    <LessonTakeaway>Prometheus scrapes /metrics endpoint.</LessonTakeaway>
                    <LessonTakeaway>Create custom counters and histograms.</LessonTakeaway>
                    <LessonTakeaway>Grafana visualizes metrics dashboards.</LessonTakeaway>
                </LessonTakeaways>
            </div>
            <LessonFooter 
                PreviousUrl="training/microservices/lesson/24" 
                PreviousText="Previous Lesson"
                NextUrl="training/microservices/lesson/26" 
                NextText="Next Lesson" />
        </article>
    </main>
</div>
