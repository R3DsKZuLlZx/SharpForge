@namespace SharpForge.Client.Components

<div class="post-tags">
    @if (Tags is not null)
    {
        foreach (var tag in Tags)
        {
            <BlogTag Text="@tag" />
        }
    }
    else
    {
        @ChildContent
    }
</div>
